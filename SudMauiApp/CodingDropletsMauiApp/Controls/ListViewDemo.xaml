<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CodingDropletsMauiApp.Controls.ListViewDemo"
             Title="ListViewDemo">

    <ListView x:Name="listView" HasUnevenRows="True" SeparatorColor="Green" SelectionMode="None">
        <!--<ListView.ItemsSource>
            <x:Array Type="{x:Type x:String}">
                <x:String>Item 1</x:String>
                <x:String>Item 2</x:String>
                <x:String>Item 3</x:String>
                <x:String>Item 4</x:String>
                <x:String>Item 5</x:String>
            </x:Array>
        </ListView.ItemsSource>-->
        <ListView.ItemTemplate>
            <DataTemplate>
                <!--<ViewCell>
                    <StackLayout BackgroundColor="Bisque" Padding="10">
                        <Label Text="{Binding Title}" TextColor="Red" FontAttributes="Bold" HorizontalOptions="Center"/>
                        <Label Text="{Binding Description}" TextColor="Red" FontAttributes="Bold" HorizontalOptions="Center"/>
                    </StackLayout>
                </ViewCell>-->

                <!--<TextCell Text="{Binding Title}" Detail="{Binding Description}"/>-->

                <!--<SwitchCell Text="{Binding Title}"/>-->

                <EntryCell Text="{Binding Title}"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>